:80 {
    handle_path /files/* {
        rewrite * /vote-oss-public{uri}
        reverse_proxy host.docker.internal:9000
        header X-Content-Type-Options "nosniff"
    }

    handle {
        reverse_proxy host.docker.internal:3000
    }
}